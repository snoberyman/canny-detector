{
  "name": "canny-detector",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run electron\"",
    "dev": "vite --host",
    "electron": "npm run build:ts && electron .",
    "build": "vite build",
    "build:ts": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^22.13.9",
    "concurrently": "^9.1.2",
    "electron": "^35.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1",
    "react-icons": "^5.5.0"
  }
}
